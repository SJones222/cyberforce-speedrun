name: validate

on:
  push:
  pull_request:

jobs:
  shell-and-static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install ShellCheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Validate repository
        run: ./validate-repo.sh
