# Managed by cyberforce-speedrun.
<VirtualHost *:80>
    DocumentRoot /var/www/html
    DirectoryIndex index.html

    <Directory /var/www/html>
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/cyberforce-error.log
    CustomLog ${APACHE_LOG_DIR}/cyberforce-access.log combined
</VirtualHost>
